openapi: 3.0.3
info:
  title: Remote Ref API
  version: 1.0.0
paths:
  /thing:
    get:
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                $ref: './other.yaml#/components/schemas/Thing'
