version: 1
consumers:
  - id: new-path-consumer
    kind: service
    contract: fixture
    operations:
      - GET /users/{userId}
  - id: old-path-consumer
    kind: frontend
    contract: fixture
    operations:
      - GET /users/{id}
