version: 1
consumers:
  - id: OpenWA
    kind: service
    contract: fixture
    operations:
      - GET /users/{id}
  - id: example-frontend
    kind: frontend
    contract: fixture
    operations:
      - GET /users/{id}
